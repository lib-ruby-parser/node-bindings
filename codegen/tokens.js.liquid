// This file is autogenerated by {{ template }}

module.exports = {
    name_to_id: {
        {%- for token in tokens %}
        {{ token.name }}: {{ token.value }},
        {%- endfor %}
    },
    id_to_name: {
        {%- for token in tokens %}
        {{ token.value }}: "{{ token.name }}",
        {%- endfor %}
    }
}
